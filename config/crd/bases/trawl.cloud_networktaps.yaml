---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: networktaps.trawl.cloud
spec:
  group: trawl.cloud
  names:
    kind: NetworkTap
    listKind: NetworkTapList
    plural: networktaps
    shortNames:
    - tap
    - taps
    singular: networktap
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyTargets
      name: Targets
      type: string
    - jsonPath: .status.matchedTargets
      name: Matched
      type: string
    - jsonPath: .status.lastPacketTime
      name: Last Packet
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkTap declares one passive observation point.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              NetworkTapSpec is the desired observation state.

              The cross-field rules are expressed as CEL so the API server enforces them.
              Leaving them to the webhook alone would let a direct etcd restore or a webhook
              outage admit a tap whose source branch does not match its type.
            properties:
              analyzers:
                description: Analyzers selects which analysis runs on the observed
                  traffic.
                properties:
                  suricata:
                    description: Suricata provides signature-based detection.
                    properties:
                      customContent:
                        description: |-
                          CustomContent optionally overlays site-specific rules or scripts on top
                          of the upstream content fetched at startup (ADR-0005).

                          Absent means upstream-only, which is the normal case.
                        properties:
                          reference:
                            description: Reference is an OCI artifact reference in
                              repository@sha256:... form.
                            maxLength: 512
                            minLength: 1
                            pattern: ^[^@\s]+@sha256:[0-9a-f]{64}$
                            type: string
                        required:
                        - reference
                        type: object
                      enabled:
                        default: false
                        description: Enabled turns this analyzer on for the tap.
                        type: boolean
                      resources:
                        description: |-
                          Resources are the requests and limits for the analyzer container.

                          Required when enabled, and validated so requests do not exceed limits.
                          An unbounded analyzer on a sensor node competes with the workloads it is
                          meant to observe.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                    type: object
                  zeek:
                    description: Zeek provides protocol metadata.
                    properties:
                      customContent:
                        description: |-
                          CustomContent optionally overlays site-specific rules or scripts on top
                          of the upstream content fetched at startup (ADR-0005).

                          Absent means upstream-only, which is the normal case.
                        properties:
                          reference:
                            description: Reference is an OCI artifact reference in
                              repository@sha256:... form.
                            maxLength: 512
                            minLength: 1
                            pattern: ^[^@\s]+@sha256:[0-9a-f]{64}$
                            type: string
                        required:
                        - reference
                        type: object
                      enabled:
                        default: false
                        description: Enabled turns this analyzer on for the tap.
                        type: boolean
                      resources:
                        description: |-
                          Resources are the requests and limits for the analyzer container.

                          Required when enabled, and validated so requests do not exceed limits.
                          An unbounded analyzer on a sensor node competes with the workloads it is
                          meant to observe.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                    type: object
                type: object
              mirrorInterface:
                description: MirrorInterface observes a physical mirror port on exactly
                  one node.
                properties:
                  interface:
                    description: |-
                      Interface is the Linux interface name to observe.

                      Bounded to 15 bytes by IFNAMSIZ, and pattern-checked because this value
                      reaches a capture process argument. Validating it here means a malformed
                      name is rejected at admission rather than becoming a pod that crash-loops.
                    maxLength: 15
                    minLength: 1
                    pattern: ^[A-Za-z0-9][A-Za-z0-9_.-]*$
                    type: string
                  nodeSelector:
                    description: |-
                      NodeSelector chooses eligible nodes.

                      Required and non-empty. An empty selector matches every node, which for a
                      mirror source would mean opening a capture socket cluster-wide instead of
                      on the one node wired to the SPAN port.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  promiscuous:
                    default: false
                    description: |-
                      Promiscuous enables promiscuous mode on the interface.

                      Required for a mirror port, which receives frames addressed elsewhere.
                    type: boolean
                required:
                - interface
                - nodeSelector
                type: object
              mode:
                default: Passive
                description: Mode is the observation mode. Passive is the only accepted
                  value.
                enum:
                - Passive
                type: string
              nodeInterface:
                description: NodeInterface observes a node interface on one or more
                  nodes.
                properties:
                  interface:
                    description: |-
                      Interface is the Linux interface name to observe.

                      Bounded to 15 bytes by IFNAMSIZ, and pattern-checked because this value
                      reaches a capture process argument. Validating it here means a malformed
                      name is rejected at admission rather than becoming a pod that crash-loops.
                    maxLength: 15
                    minLength: 1
                    pattern: ^[A-Za-z0-9][A-Za-z0-9_.-]*$
                    type: string
                  nodeSelector:
                    description: |-
                      NodeSelector chooses eligible nodes.

                      Required and non-empty. An empty selector matches every node, which for a
                      mirror source would mean opening a capture socket cluster-wide instead of
                      on the one node wired to the SPAN port.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  promiscuous:
                    default: false
                    description: |-
                      Promiscuous enables promiscuous mode on the interface.

                      Required for a mirror port, which receives frames addressed elsewhere.
                    type: boolean
                required:
                - interface
                - nodeSelector
                type: object
              type:
                description: Type selects which source branch applies.
                enum:
                - MirrorInterface
                - NodeInterface
                type: string
            required:
            - analyzers
            - type
            type: object
            x-kubernetes-validations:
            - message: type MirrorInterface requires mirrorInterface
              rule: self.type != 'MirrorInterface' || has(self.mirrorInterface)
            - message: type MirrorInterface forbids nodeInterface
              rule: self.type != 'MirrorInterface' || !has(self.nodeInterface)
            - message: type NodeInterface requires nodeInterface
              rule: self.type != 'NodeInterface' || has(self.nodeInterface)
            - message: type NodeInterface forbids mirrorInterface
              rule: self.type != 'NodeInterface' || !has(self.mirrorInterface)
            - message: at least one analyzer must be enabled
              rule: (has(self.analyzers.suricata) && self.analyzers.suricata.enabled)
                || (has(self.analyzers.zeek) && self.analyzers.zeek.enabled)
            - message: enabled suricata requires resources
              rule: '!(has(self.analyzers.suricata) && self.analyzers.suricata.enabled)
                || has(self.analyzers.suricata.resources)'
            - message: enabled zeek requires resources
              rule: '!(has(self.analyzers.zeek) && self.analyzers.zeek.enabled) ||
                has(self.analyzers.zeek.resources)'
          status:
            description: NetworkTapStatus is the observed state.
            properties:
              conditions:
                description: Conditions carry the detailed reasons behind Phase.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastPacketTime:
                description: LastPacketTime is the most recent packet across all targets.
                format: date-time
                type: string
              matchedTargets:
                description: MatchedTargets is how many nodes the selector resolved
                  to.
                format: int32
                type: integer
              observedGeneration:
                description: |-
                  ObservedGeneration is the spec generation this status describes. Status is
                  stale whenever it lags metadata.generation, and consumers must treat a
                  stale status as unknown rather than current truth.
                format: int64
                type: integer
              phase:
                description: Phase is the aggregate state.
                enum:
                - Pending
                - Active
                - Degraded
                - Error
                type: string
              readyTargets:
                description: ReadyTargets is how many are observing with healthy analyzers.
                format: int32
                type: integer
              targets:
                description: Targets reports per-node observation health.
                items:
                  description: TargetStatus reports observation health on one node.
                  properties:
                    analyzers:
                      description: Analyzers reports per-analyzer health on this target.
                      items:
                        description: AnalyzerStatus reports one analyzer's health
                          on one target.
                        properties:
                          customContentDigest:
                            description: |-
                              CustomContentDigest is the digest of the applied custom content artifact,
                              empty when the analyzer is running upstream-only.
                            type: string
                          healthy:
                            description: Healthy reflects observed liveness, not desired
                              state.
                            type: boolean
                          lastRecordTime:
                            description: LastRecordTime is when this analyzer last
                              produced a record.
                            format: date-time
                            type: string
                          name:
                            description: Name identifies the analyzer.
                            enum:
                            - Suricata
                            - Zeek
                            type: string
                          reason:
                            description: Reason is a sanitized explanation when unhealthy.
                            maxLength: 256
                            type: string
                          upstreamFetchedAt:
                            description: |-
                              UpstreamFetchedAt is when this analyzer's upstream detection content was
                              retrieved, so operators can see content currency without exec'ing into a
                              pod (FR-045).
                            format: date-time
                            type: string
                          version:
                            description: Version is the analyzer's reported version.
                            type: string
                        required:
                        - healthy
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    duplication:
                      default: Unknown
                      description: Duplication reports whether duplicate observations
                        are suspected.
                      enum:
                      - Unknown
                      - NotDetected
                      - Suspected
                      type: string
                    heartbeatTime:
                      description: HeartbeatTime is when the sensor last reported.
                      format: date-time
                      type: string
                    interface:
                      description: Interface is the interface being observed.
                      type: string
                    kernelDrops:
                      description: |-
                        KernelDrops counts packets the kernel dropped before the analyzer saw
                        them. Absent when the analyzer does not report it; zero and unknown are
                        different answers for packet-loss reporting (FR-039).
                      format: int64
                      type: integer
                    lastPacketTime:
                      description: LastPacketTime is when a packet was last observed.
                      format: date-time
                      type: string
                    nodeName:
                      description: NodeName is the node this target runs on.
                      type: string
                    packetsObserved:
                      description: PacketsObserved counts packets at the capture boundary.
                      format: int64
                      type: integer
                    podRef:
                      description: PodRef references the analyzer pod.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    rejectedRecords:
                      description: |-
                        RejectedRecords counts malformed or unsupported analyzer records. Raw
                        rejected content is never stored here, only the count (FR-016).
                      format: int64
                      type: integer
                    reporterInstance:
                      description: |-
                        ReporterInstance identifies the sensor process that wrote this status.

                        It distinguishes a restarted sensor from a stalled one: counters reset on
                        restart, and without knowing the instance changed, that reset looks like
                        traffic stopping.
                      type: string
                  required:
                  - heartbeatTime
                  - interface
                  - nodeName
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - nodeName
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
