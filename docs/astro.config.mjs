// @ts-check
import { defineConfig } from 'astro/config';
import starlight from '@astrojs/starlight';

// https://astro.build/config
export default defineConfig({
	// Required for canonical URLs and the sitemap. Without it Starlight builds
	// fine and silently ships no sitemap, which is exactly the kind of quiet
	// failure this project argues against.
	site: 'https://trawl.cloud',

	integrations: [
		starlight({
			title: 'Trawl',
			description:
				'Cloud-native network security monitoring for Kubernetes — Zeek, Suricata, and event-driven packet capture, orchestrated by CRDs.',
			favicon: '/favicon.png',
			head: [
				{
					tag: 'link',
					attrs: { rel: 'apple-touch-icon', href: '/apple-touch-icon.png' },
				},
			],
			customCss: ['./src/styles/trawl.css'],
			expressiveCode: {
				shiki: {
					// Shiki ships no LogQL grammar, and the runbook is full of LogQL.
					// Aliasing it keeps the fences honest about their language while
					// silencing a per-block build warning that nobody can act on.
					langAlias: { logql: 'plaintext' },
				},
			},
			social: [
				{
					icon: 'github',
					label: 'GitHub',
					href: 'https://github.com/shart-cloud/trawl',
				},
			],
			// The illustration is a 2.2:1 scene rather than a mark, so it reads as
			// a hero on the landing page and the header keeps the wordmark.
			sidebar: [
				// Generated from specs/ by scripts/sync-content.mjs, so it is a
				// link rather than an autogenerated directory.
				{ label: 'Quickstart', link: '/quickstart/' },
				{
					label: 'Operations',
					items: [{ autogenerate: { directory: 'operations' } }],
				},
				{
					label: 'Security',
					items: [{ autogenerate: { directory: 'security' } }],
				},
				{
					label: 'Architecture Decisions',
					items: [{ autogenerate: { directory: 'adr' } }],
				},
			],
		}),
	],
});
